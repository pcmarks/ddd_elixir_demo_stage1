defmodule TrackingWeb.OpsManagerView do
  @moduledoc """
  For the Phoenix client - the template ops_manager/index.html.eex is rendered
  (by default).

  For the Elm client, there is no rendering (response, i.e., index.json) because
  the Elm client already contains everything to display the Ops Manager's HTML.

  """
  use TrackingWeb, :view

end
