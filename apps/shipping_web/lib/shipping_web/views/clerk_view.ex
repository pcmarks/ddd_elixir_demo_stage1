defmodule ShippingWeb.ClerkView do
  @moduledoc """
  For the Phoenix client - the template clerk/index.html.eex is rendered
  (by default).

  For the Elm client, there is no rendering (response, i.e., index.json) because
  the Elm client already contains everything to display the Clerk's HTML.

  """
  use ShippingWeb, :view

end
